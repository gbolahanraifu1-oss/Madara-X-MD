module.exports={name:'time',aliases:['currenttime','timezone'],category:'search',desc:'Current time in timezone',usage:'†time [timezone]', async execute(sock,msg,args,ctx){const s=ctx.settings;const tz=args.join(' ')||'UTC';try{const now=new Date();const t=now.toLocaleTimeString('en-US',{timeZone:tz,hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:true});ctx.reply(`🕐 *Time in ${tz}:*\n\n*${t}*${s.FOOTER}`);}catch{ctx.reply(`❌ Invalid timezone. Try: UTC, Africa/Lagos, America/New_York${s.FOOTER}`);}}}; 