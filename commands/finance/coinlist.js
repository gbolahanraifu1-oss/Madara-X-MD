const axios=require('axios'); module.exports = { name: 'coinlist', aliases: ['listcoins','cryptocoins'], category: 'finance', desc: 'List top cryptocurrencies', usage: '†coinlist',
    async execute(sock, msg, args, ctx) { const s=ctx.settings; await ctx.react('💹'); try { const res=await axios.get('https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page=15&page=1'); const list=res.data.map((c,i)=>`${i+1}. *${c.symbol.toUpperCase()}* — $${c.current_price?.toLocaleString()}`).join('\n'); ctx.reply(`💹 *Top 15 Crypto:*\n\n${list}${s.FOOTER}`); } catch(e){ctx.reply(`❌ Failed: ${e.message}${s.FOOTER}`);} } };
