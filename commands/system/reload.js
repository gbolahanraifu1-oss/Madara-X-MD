module.exports={name:'reload',aliases:['reloadcmds','refreshcmds','hotreload'],category:'system',desc:'Hot-reload all command plugins without restart',usage:'†reload',ownerOnly:true, async execute(sock,msg,args,ctx){const s=ctx.settings;await ctx.react('🔄');try{const {reloadCommands}=require('../../lib/loader');const cnt=reloadCommands();ctx.reply('🔄 *Reloaded!*\n✅ '+cnt+' commands loaded.'+s.FOOTER);}catch(e){ctx.reply('❌ Reload failed: '+e.message+s.FOOTER);}}};